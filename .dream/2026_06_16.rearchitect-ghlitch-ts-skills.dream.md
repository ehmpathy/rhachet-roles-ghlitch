dream = rearchitect ghlitch .ts skill implementations

## context

promoted skills from svc-jobs to rhachet-roles-ghlitch. the .sh wrappers are solid but the .ts implementation files have code quality issues flagged by peer reviews (21 blockers).

## files to fix

- `src/domain.roles/observer/skills/query.database.ts`
- `src/domain.roles/supporter/skills/use.vpc.tunnel.ts`

## issues to address

### query.database.ts

1. redundant arg parse (already done in .sh wrapper) - simplify to read from env vars
2. failhide pattern - use proper error classes with stack traces
3. mutable variables (let, array.push) - use const and immutable patterns
4. else-if branches - use early returns and formatters map
5. reach-in import from utils/database - inject via context or use contract
6. unclear error messages - make actionable with hints

### use.vpc.tunnel.ts

1. polyglot header (#!/bin/bash + TypeScript) - use proper shebang
2. plain Error instead of ConstraintError - use semantic error classes
3. no validation when config loaded from JSON file - add schema validation
4. mutable array (resources.push) - use spread/conditional construction

## approach

option 2 from blocker: simplify .ts files
- move arg parse entirely to .sh, pass via env vars
- reduce .ts to thin operations
- use proper error classes from helpful-errors
- eliminate mutable state

## peer review files for reference

- `.behavior/v2026_05_12.operator/.reviews/5.1.execution.phase0_to_phaseN.peer-review.mech-failhides.md`
- `.behavior/v2026_05_12.operator/.reviews/5.1.execution.phase0_to_phaseN.peer-review.arch-hazards-maintenance.md`
- `.behavior/v2026_05_12.operator/.reviews/5.1.execution.phase0_to_phaseN.peer-review.ergo-friction-hazards.md`
