# blocker: stale peer review cache

## what blocks me

the peer review guard has cached blockers from iteration 5 (hash `92db09f6d4a3f65413`) that reference a file that no longer exists: `portable-ts-dispatch.acceptance.test.ts`.

the reviews report 7 blockers across 3 review files:
- r1 (mech-failhides): 4 blockers
- r7 (behavior-intent-coverage): 1 blocker
- r8 (ergo-friction-hazards): 2 blockers

all blockers reference `portable-ts-dispatch.acceptance.test.ts` which was refactored away in development.

the current test files are:
- `aws.postgres.query.acceptance.test.ts`
- `aws.ssm.param.check.acceptance.test.ts`
- `use.testdb.acceptance.test.ts`

these files don't have the issues mentioned in the stale reviews.

## what i tried

1. verified all 29 acceptance tests pass (3 suites)
2. verified types pass
3. verified lint passes
4. verified the current test files don't have the issues:
   - no snapshot-only assertions (no `toMatchSnapshot()` calls)
   - `throw new Error` only used for test assertion context (json parse)
5. attempted to mark stone as arrived — guard shows "exhausted, cached" and reports stale blockers
6. checked route files — reviews are keyed to hash `92db09f6d4a3f65413` from iteration 5

## what i need

one of:

1. **delete stale review files** — remove `.behavior/v2026_06_21.fix-ts-skills/.route/5.1.execution.from_vision.guard.review.i5.*` to allow fresh reviews on current code

2. **human approval** — approve the stone to proceed past the stale blocker cache

3. **guidance** — if there's a route command to invalidate cached reviews, please advise

## verification summary

| check | status |
|-------|--------|
| types | pass |
| lint | pass |
| acceptance tests | 29 passed, 0 failed |
| code complete | yes |
