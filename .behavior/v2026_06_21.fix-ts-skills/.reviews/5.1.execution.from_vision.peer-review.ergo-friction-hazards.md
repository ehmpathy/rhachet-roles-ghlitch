🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-21T18-49-36-830Z
   ├─ review: .behavior/v2026_06_21.fix-ts-skills/.reviews/5.1.execution.from_vision.peer-review.ergo-friction-hazards.md
   └─ summary
      ├─ 2 blockers 🔴
      └─ 1 nitpicks 🟠

---
# blocker.1: cloudwatch skill integration test is skipped when credentials missing

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.verification/rule.require.external-contract-integration-tests.md

**locations**:
- src/blackbox/portable-ts-dispatch.acceptance.test.ts:130

the cloudwatch test [t3] throws a constraint error and exits without running the actual integration test if aws credentials are not present. according to rule, 'lack of credentials is not an excuse' and an integration test must call the real service. by skipping, the external contract (aws cloudwatch) is never verified, which violates the requirement for a real integration test. the test should either use mocked credentials with a documented exception or always run with a fallback.

locations: portable-ts-dispatch.acceptance.test.ts (around lines 130-140)

**snippet**:
```ts
if (!process.env.AWS_PROFILE && !process.env.AWS_ACCESS_KEY_ID) {
  throw new ConstraintError(
    'AWS credentials required for CloudWatch test',
    {
      hint: 'run: rhx keyrack unlock --owner ehmpath --env test',
    },
  );
}
```

---

# blocker.2: cloudwatch skill output is not snapped due to variability

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.verification/rule.require.contract-snapshot-exhaustiveness.md

**locations**:
- src/blackbox/portable-ts-dispatch.acceptance.test.ts:150

the cloudwatch skill output is verified only with loose structural assertions (contains '🐈', contains 'query.cloudwatch.logs') rather than a full snapshot. the test notes that output varies by aws account state and deliberately avoids snapshotting. but the rule requires every user-faced output variant to be snapped. a non-deterministic output should still be captured via a stable representation or an error-path snapshot. without a snapshot, drift or broken output in different environments goes undetected.

locations: portable-ts-dispatch.acceptance.test.ts (around line 150)

**snippet**:
```ts
then('output structure is valid', () => {
  // validate the output has expected structure elements
  // the skill uses treestruct format with mascot + artifact headers
  expect(cloudwatchOutput.output).toContain('🐈');
  // .note = cloudwatch output varies by AWS account state (log groups, timestamps)
  //         so we verify structure elements rather than snapshot the full output
  //         the key assertion is: skill ran without "module not found" = tsx dispatch works
});
```

---

# nitpick.1: comment starts with capital letter

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/lang.tones/rule.prefer.lowercase.md.min

**locations**:
- src/blackbox/portable-ts-dispatch.acceptance.test.ts:22

several comments in the test file start with a capital letter, violating the prefer-lowercase rule. comments should be entirely lowercase (except for proper nouns or code references) to keep language neutral and machine-aligned.

example: '// Create isolated temp directory with git repo' should be '// create isolated temp directory with git repo'.

**snippet**:
```ts
// Create isolated temp directory with git repo
```
