🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-21T18-44-53-410Z
   ├─ review: .behavior/v2026_06_21.fix-ts-skills/.reviews/5.1.execution.from_vision.peer-review.mech-failhides.md
   └─ summary
      ├─ 4 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Plain Error thrown without proper error class or context

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/pitofsuccess.errors/rule.require.failloud.md

**locations**:
- src/blackbox/portable-ts-dispatch.acceptance.test.ts:90

In the [t4] test block, when `rhachet init` unexpectedly succeeds, a `new Error(...)` is thrown. This violates the failloud rule which requires using proper error classes (`ConstraintError`, `MalfunctionError`, etc.) with actionable hints. A plain `Error` provides no context about who should fix it (caller vs server) and no hint for resolution. Use `ConstraintError` with a hint directing the developer to check the role name.

**snippet**:
```ts
            throw new Error(
              'expected rhachet init to fail for nonexistent role',
            );
```

---

# blocker.2: Snapshot-only assertion in `then('init output matches snapshot', ...)`

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/pitofsuccess.errors/rule.forbid.failhide.md

**locations**:
- src/blackbox/portable-ts-dispatch.acceptance.test.ts:38

This then block contains only `expect(initResult.output).toMatchSnapshot()` with no other assertions. Per the failhide rule, `expect(result).toMatchSnapshot() alone` is forbidden because it provides no explicit verification — the snapshot could be outdated or incorrect. Add at least one structural assertion (e.g., checking that the output contains expected text) alongside the snapshot to ensure the output is as intended.

**snippet**:
```ts
          expect(initResult.output).toMatchSnapshot();
```

---

# blocker.3: Snapshot-only assertion in `then('error output matches snapshot', ...)`

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/pitofsuccess.errors/rule.forbid.failhide.md

**locations**:
- src/blackbox/portable-ts-dispatch.acceptance.test.ts:119

This then block contains only `expect(errorResult.error).toMatchSnapshot()` with no other assertions. Same issue as above: snapshot alone is a failhide pattern. Add an explicit assertion verifying the error contains role-related content (e.g., `expect(errorResult.error).toMatch(/role|not found/i)`) before the snapshot.

**snippet**:
```ts
          expect(errorResult.error).toMatchSnapshot();
```

---

# blocker.4: Snapshot-only assertion in `then('help output matches snapshot', ...)`

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/pitofsuccess.errors/rule.forbid.failhide.md

**locations**:
- src/blackbox/portable-ts-dispatch.acceptance.test.ts:126

This then block contains only `expect(helpOutput.output).toMatchSnapshot()` with no other assertions. Same failhide pattern: snapshot alone provides no explicit verification. Add at least one assertion checking the output contains expected content (e.g., `expect(helpOutput.output).toContain('query.cloudwatch.logs')`) before the snapshot.

**snippet**:
```ts
          expect(helpOutput.output).toMatchSnapshot();
```
