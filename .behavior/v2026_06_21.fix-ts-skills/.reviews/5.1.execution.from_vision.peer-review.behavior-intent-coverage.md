🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-21T18-48-36-685Z
   ├─ review: .behavior/v2026_06_21.fix-ts-skills/.reviews/5.1.execution.from_vision.peer-review.behavior-intent-coverage.md
   └─ summary
      ├─ 1 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Missing snapshot for cloudwatch command output variant

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.verification/rule.require.contract-snapshot-exhaustiveness.md

**locations**:
- src/blackbox/portable-ts-dispatch.acceptance.test.ts:103-139

The acceptance test for the cloudwatch skill (t3) does not include a snapshot of the command output. The rule requires exhaustive snapshot coverage for all output variants of every user-faced contract. The test replaces the snapshot with loose structure assertions (toContain) and a justification that output varies by AWS account state. Per verification strictness philosophy, 'zero exceptions' and 'exhaustive means: every positive path, every negative path, every edge case'. Skipping a snapshot for this variant leaves a gap: drift in the output format (e.g., missing mascot, changed header) would go undetected in reviews. Either snapshot a deterministic portion of the output or find a way to make the output deterministic (e.g., by using a fixed test fixture).

**snippet**:
```ts
      when('[t3] cloudwatch skill runs via symlinked role', () => {
        // this test proves the fix works end-to-end:
        // shell skill -> tsx dispatch -> .ts file executes successfully
        // cloudwatch skill has a .ts file that is dispatched via tsx
        const cloudwatchOutput = useBeforeAll(async () => {
          // fail fast if no aws credentials
          if (!process.env.AWS_PROFILE && !process.env.AWS_ACCESS_KEY_ID) {
            throw new ConstraintError(
              'AWS credentials required for CloudWatch test',
              {
                hint: 'run: rhx keyrack unlock --owner ehmpath --env test',
              },
            );
          }

          // let execSync throw on failure - no failhide
          // the skill may or may not find log groups based on aws account state
          const output = execSync(
            'npx rhx query.cloudwatch.logs --list --env test --prefix svc-',
            {
              cwd: scene.tempDir,
              stdio: 'pipe',
              env: {
                ...process.env,
                PATH: process.env.PATH,
              },
            },
          );

          return { output: output.toString() };
        });

        then('skill executes without error', () => {
          expect(cloudwatchOutput.output).toBeDefined();
          expect(cloudwatchOutput.output.length).toBeGreaterThan(0);
        });

        then('output confirms skill ran', () => {
          expect(cloudwatchOutput.output).toContain('query.cloudwatch.logs');
        });

        then('output structure is valid', () => {
          // validate the output has expected structure elements
          // the skill uses treestruct format with mascot + artifact headers
          expect(cloudwatchOutput.output).toContain('🐈');
          // .note = cloudwatch output varies by AWS account state (log groups, timestamps)
          //         so we verify structure elements rather than snapshot the full output
          //         the key assertion is: skill ran without "module not found" = tsx dispatch works
        });
      });
```
