# blocker — 5.1.execution.from_vision

## what blocks me

the peer-review gauntlet is non-convergent l1 noise, and the one review demand i cannot
satisfy is **hermetic-impossible** by the repo's own rules. i need a wisher call to break
the loop.

concretely, three items need your adjudication:

### 1. positive ssm path (prep/prod) → exit 0 — hermetic-impossible

r8 (behavior-intent-coverage) demands integration cases where valid prep/prod config drives
the ssm tunnel to a clean exit 0. i tried it: case10 (prep) + case11 (prod) with full valid
config. the skill's own derivation passed every assertion (it emits the correct per-env
bastion/cluster/host/port/account, raises no belay). but the run then **hung past 420s** —
with valid config the skill reaches `declastruct apply`, which opens a live ssm session and
blocks on a real aws network connection.

- a live run violates `rule.require.hermetic-tests`.
- a stub of declastruct violates `rule.forbid.integration.mocks`.

so the slice is acceptance-test territory (real aws + live bastion), the same boundary the
extant `use.rds.capacity` test also does not cross. r8's own rule permits deferral **with
wisher approval**. i request that approval.

what IS covered hermetically (the skill's own responsibility — per-env derivation):
- case3: test → localhost short-circuit (exit 0, positive)
- case4: prep → ssm path, derives keys, failfast on null config
- case7: prod → ssm path, accepts prod, derives keys, failfast on null config

### 2. snapshot policy — a direct reviewer contradiction

in the same i3 round two reviewers gave opposite instructions:
- r2 (mech-failhides): a `then` with only `.toMatchSnapshot()` is a failhide.
- r9 (ergo-friction): every user-faced output variant MUST carry a snapshot.

i satisfied both: every snapshot sits beside real behavioral assertions (exit code, message
content, negative checks), and i added the absent `--help`-variant snapshot. please confirm
this is the intended policy.

### 3. ACCESS fallback in use.rds.capacity — keep or drop?

the composer keeps a prior backwards-compat `ACCESS` env fallback below its arg loop. it
predates this change. keep for compat, or drop as yagni?

## what i tried

- **creds failhide** (i1, r1/r6): moved the cred-source step to the ssm path only, strict,
  no failhide — a localhost-only dev opens the tunnel credential-free. FIXED.
- **unknown-arg silent no-op** (i2, r9): both skills now reject unknown args with belay +
  exit 2; case8 covers a removed `--bastion`. FIXED.
- **composer + tunnel snapshots** (i2/i3, r8/r9): every output variant snapped, the
  `--help`-after-args passthrough included. FIXED.
- **host null-check** (i1): failfast before the ssm path when local host is absent; case9
  covers it. FIXED.
- **positive ssm cases** (i3, r8): attempted, proven hermetic-impossible (above), reverted.

the gauntlet does not converge: blocker count went 6 → 4 → 6 across three rounds, and the
same reviewers flip verdicts (r1 reject/approve/reject, r4 reject/approve/approve). i3 even
holds a direct contradiction (r2 vs r9 on snapshots).

## what i need

a wisher decision on the three items above. all deterministic gates are green:
- vpc tunnel integration: 49 passed (hermetic, 5s)
- rds.capacity integration: 27 passed (3s)
- types, format, lint: all passed

if you concur, `--as approved` overrides the non-convergent gauntlet; otherwise point me at
whichever of the three you want handled a different way.
