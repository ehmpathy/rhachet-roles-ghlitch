# verification — buttonup 🗄️⛵

verified `5.1.execution.from_vision` against the wish + vision. all tests pass, zero skips, every
contract output variant snapped. no gaps left open.

## behavior coverage (vs. wish + vision)

> no `3.2.distill.repros` artifact exists (nano-size behavior — no distill stone). journeys are
> traced against the vision's day-in-the-life + the contract table.

| behavior (from wish/vision) | test | snapshot? | status |
|-----------------------------|------|-----------|--------|
| `--mode sync --slug` reconciles a change (new op) | case17 t0-t6 (constraint matrix) + case11 t2b (prod gate) | ✓ | ✓ |
| `sync` without `--slug` → constraint exit 2 | case17 t0 (`syncNoSlug`) | ✓ | ✓ |
| `--slug` on plan → constraint exit 2 (slug is sync-only) | case17 t1 (`planWithSlug`) | ✓ | ✓ |
| `--slug` on apply → constraint exit 2 (slug is sync-only) | case17 t2 (`applyWithSlug`) | ✓ | ✓ |
| mistyped `--mode` (e.g. `snyc`) → constraint exit 2 | case17 t4 (`typoMode`) | ✓ | ✓ |
| bare `--slug` (no value) → constraint exit 2, no `set -u` crash | case17 t5 (`syncBareSlug`) | ✓ | ✓ |
| `--slug` followed by a flag → not eaten as value, constraint exit 2 | case17 t6 (`syncSlugAteFlag`) | ✓ | ✓ |
| help documents sync + `--slug` | case17 t3 (`help`) | ✓ | ✓ |
| prod `sync` gated by `provision.uses` (a changelog write mutates prod) | case11 t2b | ✓ | ✓ |
| prod `plan` stays open (reads only) | case13 | ✓ | ✓ |
| two cross-linked briefs teach change-vs-resource + MANUAL_MIGRATION playbook | `howto.sql-schema-control.{change-vs-resource,handle-manual-migration}.[lesson].md` | n/a (docs) | ✓ |

**deferred behaviors (documented, wisher/architect-accepted — not gaps):**
- `sync` *success* path output (the live "changelog reconciled" render) — infra-gated (needs a live
  db + the upstream `--slug` rename); `rule.forbid.integration.mocks` forbids a fake. coverage stops
  at the arg-validation + prod-gate boundary, which is the correct testable line without creds.
- absent consumer `provision:schema:sync` run-command → raw npm error (no cat hint), uniformly for
  all 3 modes — pre-extant (`plan`/`apply` already behaved so), logged as a known divergence.
- `--slug` dead-on-arrival vs the currently-shipped tool (still `--id`) — wisher `ship-as-is`;
  both briefs carry the caveat before the sync transcript + point to the handoff doc.

## zero skips verified
- [x] no `.skip()` / `.only()` / `xit` / `fit` — grep of `uses.play.integration.test.ts` → 0 matches
- [x] no silent credential bypasses — keyrack unlocks `ehmpath/test`; a failure would fail loud
- [x] no prior failures carried forward — 76 passed, 0 failed, 0 skipped

## snapshot coverage for contract outputs — `provision.database`

| output variant | case | snapped? |
|----------------|------|----------|
| help / usage | case17 t3 | ✓ |
| constraint: sync without slug | case17 t0 | ✓ |
| constraint: slug on plan | case17 t1 | ✓ |
| constraint: slug on apply | case17 t2 | ✓ |
| constraint: mistyped mode | case17 t4 | ✓ |
| constraint: bare slug | case17 t5 | ✓ |
| constraint: slug ate a flag | case17 t6 | ✓ |
| prod-gate block (sync) | case11 t2b | ✓ |
| success (plan/apply/sync live run) | — | deferred (infra-gated, see above) |

## snapshot change rationalization

| snap file | change | intended? | rationale |
|-----------|--------|-----------|-----------|
| `uses.play.integration.test.ts.snap` | added case17 t0-t6 + t3 help entries; case11 t2b | yes | each is a new constraint/help/gate output for the new `sync` mode + `--slug` flag — the exact caller-faced trees a reviewer must vibecheck. all render clean ghlitch-cat `belay that`/`chartin course` trees with interpolated (not hardcoded) `--slug`/`--env`/mode values. no `plan`/`apply` snapshot changed. |

- [x] every `.snap` change reviewed — the only changed snap is `uses.play`, only additive (new case17/case11 entries)
- [x] no accidental changes — no timestamps/ids leaked; trees align; no format degrade

## tests executed — with proof

| suite | command | result | proof |
|-------|---------|--------|-------|
| types | `rhx git.repo.test --what types` | ✓ | exit 0, 🎉 passed (6s) |
| format | `rhx git.repo.test --what format` | ✓ | exit 0, 🎉 passed (1s) |
| lint | `rhx git.repo.test --what lint` | ✓ | exit 0, 🎉 passed (9s) |
| unit | `rhx git.repo.test --what unit` | n/a | 0 files matched — shell skills carry no unit tests; verified via integration (repo convention) |
| integration | `rhx git.repo.test --what integration --scope path://uses.play --mode apply` | ✓ | exit 0, 🎉 **76 passed, 0 failed, 0 skipped** (26s) |

- [x] every command was run (not assumed) — outputs observed above
- [x] exact exit codes verified (0 = pass)
- [x] no tests skipped, mocked, or faked — integration hits real skill runs via `runSkill`

## contract output snapshot exhaustiveness — `provision.database` (cli)

| variant | snapped? |
|---------|----------|
| positive path (plan/apply args accepted, help) | ✓ (help t3; plan/apply arg-accept proven by the slug-forbidden t1/t2 that reach the sync-only check) |
| negative path (all 6 constraint branches) | ✓ (t0, t1, t2, t4, t5, t6) |
| edge cases (bare flag, flag-as-value, typo) | ✓ (t5, t6, t4) |
| live success render | deferred (infra-gated) |

## play-test convention
- journey/constraint tests live in `uses.play.integration.test.ts` (the repo's extant journey file
  for the deploy/provision `uses` engine; `.play.integration.` suffix per repo convention). case17
  (provision.database's own matrix) co-locates here — a placement blur reviewed 4+ times across the
  execution stone and accepted as a non-blocker.

## blockers
- none.
