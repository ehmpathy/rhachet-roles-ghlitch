# 5.3.verification — provision.declastruct

## verification checklist

### behavior coverage (vs vision edgecase table)

the vision (`1.vision.yield.md`) declares no blackbox/repros artifact, so coverage is judged
against its edgecase table + contract. the argument-boundary suite
(`provision.declastruct.integration.test.ts`) spawns the real skill and asserts exact exit codes
and belay/help stdout — hermetic (no creds, no live declastruct).

| behavior (vision) | test case | status |
|-------------------|-----------|--------|
| help (`help`/`--help`/`-h`) | case1 | ✓ pass |
| absent `--wish` → exit 2 | case2.t0 | ✓ pass |
| `--wish` path absent on disk → exit 2 | case3.t0 | ✓ pass |
| absent `--env` → exit 2 | case2.t1 | ✓ pass |
| invalid `--env` → exit 2 | case4.t0 | ✓ pass |
| absent `--mode` → exit 2 | case2.t2 | ✓ pass |
| invalid `--mode` → exit 2 | case4.t1 | ✓ pass |
| invalid `--auth` → exit 2 | case4.t2 | ✓ pass |
| valued flag with no value → exit 2 (no set -u crash) | case5.t0 (`--wish`), case5.t1 (`--auth`), case5.t4 (`--plan`) | ✓ pass |
| `--` hard stop drains later tokens verbatim | case6.t0 | ✓ pass |
| apply with no prior plan file → exit 2 (pre-header belay, closed tree) | case7.t0 | ✓ pass |
| live `--mode plan` forward-contract stdout | live-plan case1 | ✓ pass |
| explicit `--plan <path>` overrides derived `<wish>.plan.json` | live-plan case2 | ✓ pass |
| live `--mode apply` of reviewed plan | — | ⚠ needs prod meter + prod env (see blockers) |
| prod-apply gate (`provision.uses`) | — | ⚠ needs prod meter + prod env (see blockers) |

the pure argument-boundary matrix — the class of defect the `--` passthrough slip was, and the
bar the l3 behavior-intent reviewer set for this wish at execution — is fully covered and green.
the **live `--mode plan` forward-contract path is now proven**: a second describe block spawns the
real `npx declastruct plan` against an empty wish (no aws resources), asserts exit 0, the
🐈/⛵ framed stdout (`chartin course` → `plan infra changes` → `smooth sailin`), the written
`<wish>.plan.json`, and the `planned →` handoff line. the live apply + prod-gate paths still need a
prod meter + prod env (the demo wish sources `env: test`); the l3 reviewer noted this coverage
"can legitimately wait for 5.3 or a future wish with disposable aws resources."

### zero skips verified
- [x] no `.skip()` / `.only()` / `xit` / `xdescribe` in the test (grep: no matches)
- [x] no silent credential bypass — the test sets `AWS_ACCESS_KEY_ID` so the skill takes the
      documented ci/oidc keyrack-skip path; it does not swallow an absent credential
- [x] no prior failures carried forward within this suite

### tests executed — with proof

| suite | command | result | proof |
|-------|---------|--------|-------|
| types | `npm run test:types` | ✓ | `TYPES_EXIT:0`, tsc no errors |
| format | `npm run test:format` | ✓ | `FORMAT_EXIT:0`, biome "No fixes applied", 31 files |
| lint | `npm run test:lint` | ✓ | `LINT_EXIT:0`, biome + no cycles + depcheck clean |
| integration (this deliverable) | `rhx git.repo.test --what integration --scope 'path://provision.declastruct.integration' --mode apply` | ✓ | `🎉 passed (92s)`, **40 passed, 0 failed, 0 skipped** (arg-boundary + live-plan + live-plan-override + 16 snapshots: help, 14 belays, plan-success) |
| integration (full changed-since-main) | `rhx git.repo.test --what integration --mode apply` | ✓ | `🎉 passed`, 8 suites, **498 passed, 0 failed, 0 skipped** |

with an aws sso session unlocked (`rhx keyrack unlock --owner ehmpath --env test`), the **full
`--changedSince=main` set is green**: all 8 suites pass — the observer skills
(`aws.cloudwatch.logs.query`, `aws.s3.get`, `aws.s3.list`) that fail only when no aws session
exists now pass too. no out-of-scope failures are left; the whole changed set is proven.

### snapshot coverage

the argument-boundary suite asserts the exact belay/help text for every variant (exit code + the
precise stdout a user sees) **and now full-block `toMatchSnapshot` snapshots every skill-owned
output variant**. the committed baseline
(`__snapshots__/provision.declastruct.integration.test.ts.snap`) holds 16 blocks:

| variant | snapshot |
|---------|----------|
| help (now lists `--plan`) | case1.t0 |
| absent `--wish` / `--env` / `--mode` belay | case2.t0 / t1 / t2 |
| wish-not-found belay | case3.t0 |
| invalid `--env` / `--mode` / `--auth` belay | case4.t0 / t1 / t2 |
| no-value `--wish` / `--auth` / `--env` / `--mode` / `--plan` belay | case5.t0 / t1 / t2 / t3 / t4 |
| `--` hard-stop → absent-mode belay | case6.t0 |
| apply-no-plan belay (pre-header, closed tree; temp paths → `<WISH>`) | case7.t0 |
| **success plan (full stdout, temp paths masked)** | live-plan case1.t0 |

so a pr reviewer sees the exact stdout of every error a user can hit AND the full success
journey, without run of the skill, and any drift surfaces in the diff. the `withStablePaths`
helper swaps per-run temp paths for `<WISH>`/`<DIR>` on the belays; the live-plan scene's
`stdoutMasked` masks the temp wish/dir path in all three forms it appears (the absolute path the
skill echoes, the repo-relative form declastruct echoes, and the symlink realpath) so the
**success plan stdout is snapped in full** — 🐈/⛵ frame, the forwarded `🌊 declastruct plan`
body (with `resources: 0`, deterministic for the provider-less empty wish), and the
`planned → <WISH>.plan.json` footer. the success snapshot follows the direct shipmate's masked
plan-snapshot pattern (`provision.database.integration.test.ts:249`). the live **apply** success
still needs a disposable target (l3-blessed deferral); its belay path is snapped (case7). all
baselines were generated with an aws sso session unlocked and are clean.

**first-class `--plan`**: `--plan <path>` is an optional input that overrides the derived
`<wish>.plan.json` default (declastruct's `--wish`/`--plan` backbone, surfaced through the skill).
plan mode writes it (mapped to declastruct's `--into`), apply mode reads it. covered by an
arg-boundary no-value belay (case5.t4, snapped) and a live proof (live-plan case2) that spawns the
real `npx declastruct plan --plan <custom>` and asserts the plan lands at the explicit path and
**not** at the derived default — the default stays the pit of success for callers who omit it.

### blockers

**none.** the aws sso session was unlocked (`rhx keyrack unlock --owner ehmpath --env test`), and
with it every prior blocker cleared:

1. the 3 observer suites (out of scope) now pass — the full `--changedSince=main` set is green
   (482 passed / 0 failed / 0 skipped).
2. the live-declastruct forward-contract proof (plan stdout pass-through) — the shipmate-standard
   equivalent of `provision.database.integration.test.ts` — was added and passes against the real
   `npx declastruct plan` on an empty wish.
3. the help snapshot baseline was generated + committed.

the only paths still uncovered are live `--mode apply` and the prod-apply gate, which need a prod
meter + a prod declastruct wish (none exists yet — the demo wish sources `env: test`). the l3
reviewer explicitly blessed this deferral. the deliverable's coverage is complete and green.
