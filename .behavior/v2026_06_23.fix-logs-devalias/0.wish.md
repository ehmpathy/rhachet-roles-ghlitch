wish =

hey, we have lots of services which still deploy lambdas via `$service-$env-$function` where env is dev instead of prep

so, when someone does `aws.cloudwatch.query --env prep`, we want to alias to both -dev and -prep

since -dev is an historic alias to -prep

