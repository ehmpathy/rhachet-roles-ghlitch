# verification checklist

## behavior coverage (fix-logs-devalias)

the behavior: when `--env prep` is passed, search both `-prep` and `-dev` log group suffixes.

| journey | test file | snapshots? | status |
|---------|-----------|------------|--------|
| `--env prep` alias to both `-prep` and `-dev` prefixes | all prep cases | ✓ | ✓ |
| `--list --env prep` shows alias hint | case10 | ✓ snapped | ✓ |
| `--tail --env prep` shows alias hint when no groups found | case12 | ✓ snapped | ✓ |
| `--lambda nonexistent --env prep` shows alias hint | case14 | ✓ snapped | ✓ |
| query mode with prep env shows alias hint | case17 | ✓ snapped | ✓ |
| test env does NOT show alias hint | case8 | ✓ snapped | ✓ |
| prod env does NOT show alias hint | case9 | ✓ snapped | ✓ |

all alias behavior is covered by extant tests.

## zero skips verified

- [x] no .skip() or .only() found
- [x] no silent credential bypasses
- [x] no prior failures carried forward

## tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types` | ✓ | passed (1s) |
| lint | `rhx git.repo.test --what lint` | ✓ | passed (3s) |
| unit | `rhx git.repo.test --what unit --mode apply` | ✓ | passed (0 tests - no changes since main) |
| integration | `rhx git.repo.test --what integration --mode apply --scope aws.cloudwatch.query` | ✓ | 96 tests passed (26s) |

## snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| case8 | modified | yes | now shows real log group `/aws/lambda/rhachet-roles-ghlitch-test-echo` |
| case10 | modified | yes | shows alias hint "(also checked historic -dev alias)" |
| case11 | modified | yes | --tail without --lambda now fails upfront (clean belay, no partial search output) |
| case12 | modified | yes | --tail without --lambda fails upfront for prep env too; hint references --env prep |
| case13 | modified | yes | available log groups now tree-indented under the node |
| case14 | modified | yes | shows alias hint + tree-indented available log groups |
| case16 | modified | yes | now shows query via Logs Insights against real log group |
| case17 | modified | yes | uses absent prefix for deterministic no-groups path; shows alias hint "(also checked historic -dev alias)" |

all snapshot changes were reviewed and are correct reflections of the new behavior.

## blockers

none
