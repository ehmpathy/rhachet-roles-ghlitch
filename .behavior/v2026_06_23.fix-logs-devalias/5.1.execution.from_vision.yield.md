# execution: fix-logs-devalias

## scope

per vision + wisher answers:

1. `aws.cloudwatch.query.sh`
   - [x] query mode: fallback from `-prep` to `-dev`
   - [x] list mode: search both `-prep` and `-dev`
   - [x] emit hint when alias is used

2. `query.cloudwatch.metrics.sh`
   - [x] lambda queries: fallback from `-prep` to `-dev`
   - [x] SQS queries: fallback from `-prep` to `-dev`
   - [x] emit hint when alias is used

## changes made

### aws.cloudwatch.query.sh

added `get_env_suffixes()` helper that returns `"prep dev"` for prep env, else just the env.

modified:
- list mode: searches both suffixes, dedupes, tracks USED_ALIAS
- query mode (single lambda): tries each suffix until found, tracks USED_ALIAS
- query mode (multi-group): collects log groups from all suffixes
- output: shows "(found via historic -dev alias)" when USED_ALIAS set

### query.cloudwatch.metrics.sh

added same `get_env_suffixes()` helper.

modified:
- SQS list: searches both suffixes, dedupes, tracks USED_ALIAS
- lambda list: searches both suffixes via associative array to track full names
- single lambda: tries each suffix until found, tracks USED_ALIAS
- output: shows "(found via historic -dev alias)" when USED_ALIAS set

## verification

### integration tests

- [x] 85 tests pass
- [x] case7: isolated HOME via genTempDir to test keyrack failure
- [x] snapshots stable (maskDynamicOutput handles genTempDir paths)

### test fixes

- [x] jest.integration.config.ts: fixed testMatch pattern (`**/src/**/*` instead of `src/**/*`)
- [x] jest.acceptance.config.ts: same fix applied
