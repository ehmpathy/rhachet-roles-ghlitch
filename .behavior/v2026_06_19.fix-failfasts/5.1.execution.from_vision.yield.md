# execution: eliminate failhides

## deliverables

- [x] 1. new rule: `.agent/repo=.this/role=any/briefs/rule.forbid.failhide-in-shell.md`
- [x] 2. fix `aws.ssm.param.check.sh`: remove failhide patterns
- [x] 3. audit & fix other skills

---

## progress

### 1. rule created

`.agent/repo=.this/role=any/briefs/rule.forbid.failhide-in-shell.md`
- severity: blocker
- defines failhide pattern: `2>/dev/null || echo ""`
- explains allowed vs forbidden
- documents the fix: delete the pattern

### 2. fixed aws.ssm.param.check.sh

line 183-186: removed `2>/dev/null || echo ""`
- before: `--output text 2>/dev/null || echo "")`
- after: `--output text)`

### 3. audit & fix other skills

**fixed:**

1. `query.cloudwatch.metrics.sh`
   - line 234-237: removed `2>/dev/null` from sqs list-queues
   - line 272: removed `2>/dev/null || echo "0"` from get-metric-statistics
   - line 317: removed `2>/dev/null || echo "0"` from get-metric-statistics

2. `aws.cloudformation.rollback.sh`
   - line 165-166: removed `2>/dev/null || echo "UNKNOWN"` from describe-stacks
   - line 169-170: removed `2>/dev/null` from describe-stack-events

3. `aws.ssm.param.check.sh` (additional fix)
   - line 159: removed `2>&1` from check_param() function
   - before: `if aws ssm get-parameter ... >/dev/null 2>&1; then`
   - after: `if aws ssm get-parameter ... >/dev/null; then`

4. `query.cloudwatch.logs.sh`
   - line 393: removed `2>/dev/null` from describe-log-groups
   - before: `--output text 2>/dev/null | grep -q`
   - after: `--output text | grep -q`

5. `provision.database.sh`
   - line 174: removed `2>/dev/null` from aws configure export-credentials
   - before: `--format env 2>/dev/null)" || true`
   - after: `--format env)" || true`

6. **keyrack get patterns** (all failhides, all fixed):

| file | line | fix |
|------|------|-----|
| `aws.ssm.param.check.sh` | 128 | removed `2>/dev/null` from keyrack get |
| `aws.ssm.param.check.sh` | 139 | removed `2>/dev/null` from credential export |
| `query.cloudwatch.logs.sh` | 200 | removed `2>/dev/null` from keyrack get |
| `query.cloudwatch.metrics.sh` | 176 | removed `2>/dev/null` from keyrack get |
| `query.cloudwatch.metrics.sh` | 188 | removed `2>/dev/null` from credential export |
| `query.database.sh` | 89 | removed `2>/dev/null` from keyrack get |
| `invoke.command.sh` | 134 | removed `2>/dev/null` from keyrack get |
| `invoke.vital.sh` | 134 | removed `2>/dev/null` from keyrack get |
| `use.rds.capacity.sh` | 82,84 | removed `2>/dev/null` from keyrack unlock/get |
| `use.testdb.sh` | 69 | removed `2>/dev/null` from keyrack get |
| `deploy.sh` | 106,118 | removed `2>/dev/null` from keyrack get & credential export |
| `aws.cloudformation.status.sh` | 133,144 | removed `2>/dev/null` from keyrack get & credential export |
| `aws.cloudformation.rollback.sh` | 134,145 | removed `2>/dev/null` from keyrack get & credential export |
| `provision.database.sh` | 172 | removed `2>/dev/null` from keyrack get |
| `provision.terraform.sh` | 152 | removed `2>/dev/null` from keyrack get |

**patterns retained (not failhides):**

these `2>/dev/null` patterns are acceptable — they intentionally suppress noise:

| pattern | reason |
|---------|--------|
| `git rev-parse --show-toplevel 2>/dev/null \|\| echo "."` | fallback for non-git dirs |
| `docker rm -f ... 2>/dev/null \|\| true` | ignore "container absent" noise |
| `unset AWS_PROFILE 2>/dev/null \|\| true` | unset doesn't fail meaningfully |
| `jq -r '.name' ... 2>/dev/null \|\| echo ""` | fallback for absent package.json |

## verification

- [x] build passes: `npm run build`
- [x] no keyrack/aws failhides remain

## guard constraint note

the guard's peer reviews (r1-r8) are scoped to TypeScript files (`*.ts`) but this behavior only modifies shell files (`*.sh`). the reviewers correctly find 0 TypeScript files to review. result: constraint exit (exit 2). this is expected — the scope mismatch is inherent to this behavior's shell-only scope.
