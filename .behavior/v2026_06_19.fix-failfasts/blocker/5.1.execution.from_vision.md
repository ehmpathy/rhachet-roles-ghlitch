# blocker: guard peer reviews scoped to typescript, behavior changes shell

## what blocks me

the execution guard has peer reviews (r1-r8) scoped to TypeScript files (`*.ts`) via `--diffs since-main`. however, this behavior ONLY modifies shell files (`*.sh`).

when the reviewers run, they find 0 TypeScript files changed, which causes a constraint exit (exit 2). even though the reviews show "0 blockers", the constraint exit is treated as a blocker.

## what i tried

1. ran `--as passed` multiple times — blocked by "reviewer constraint"
2. ran `--as arrived` — same result
3. verified all reviews show "approved" with "0 blockers ✓"
4. the actual review commands exit 2 (constraint) not exit 1 (failure)

## what i need

human approval to pass this stone. the work is complete:

- all failhide patterns eliminated from shell skills
- rule added at `.agent/repo=.this/role=any/briefs/rule.forbid.failhide-in-shell.md`
- build passes
- no keyrack/aws failhides remain

alternatively, the guard could be updated to scope reviews to `*.sh` files for shell-only behaviors, or to treat constraint exits (exit 2) as pass-through rather than blockers.
