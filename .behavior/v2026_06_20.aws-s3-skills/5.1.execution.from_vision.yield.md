# execution: aws.s3.list and aws.s3.get skills

## todos

- [x] create aws.s3.list.sh in observer role
- [x] create aws.s3.get.sh in observer role
- [x] update observer readme with new skills
- [ ] verify skills work (manual test by human)

## changes from source

| aspect | source (app-storefronts-web) | target (ghlitch/observer) |
|--------|------------------------------|---------------------------|
| artifact | `🦺` operator | `🔮` observer |
| cache path | `repo=.this` | `repo=ghlitch` |
| --uri | not supported | preferred input |
| --env | default: prod | required, no default |

## progress

### created files

1. `src/domain.roles/observer/skills/aws.s3.list.sh` — list s3 bucket contents
2. `src/domain.roles/observer/skills/aws.s3.get.sh` — fetch s3 object contents
3. updated `src/domain.roles/observer/readme.md` — added new skills to table and examples
4. `.agent/repo=.this/role=any/briefs/rule.forbid.default-env.md` — rule to forbid defaulted --env

## self-review findings

### r1: has-pruned-yagni — pass
no unnecessary features found.

### r2: has-pruned-backcompat — pass
no backward compat hacks found.

### r3: has-consistent-mechanisms — pass (after fixes)

**issue found**: keyrack credential pattern had two deviations:
- `aws.s3.get.sh`: absent `--value` flag, added `2>/dev/null` (failhide)
- `aws.s3.list.sh`: added `2>/dev/null` (failhide)

**fix applied**: aligned with extant pattern — removed `2>/dev/null`, added `--value` flag

### r4: has-consistent-conventions — pass
skill names, variable names, argument names, error messages, help output all follow extant patterns.
