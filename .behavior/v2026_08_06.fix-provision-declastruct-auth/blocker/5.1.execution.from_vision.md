# blocker — 5.1.execution.from_vision — ✅ CLEARED (2026-08-09)

> **status: resolved.** vlad completed the browser sso prompt; `rhx keyrack unlock --owner
> ehmpath --env test` authenticated as `demo-agent` (540m), and the resnap ran.
>
> | suite | asserted | committed | result |
> |---|---|---|---|
> | `provision.declastruct.integration.test.ts` | 23 | **23** | PASS |
> | `uses.play.integration.test.ts` | 52 | **52** | PASS |
>
> full run: **550 passed, 4 failed** — the 4 are the pre-existent pnpm-TTY set, all in
> `provision.database.integration.test.ts`, a suite this change never edits. confirmed by a
> grep of this run's own stderr, never assumed from the earlier control.
>
> **the resnap also caught a defect of mine.** the first pass produced three host-dependent
> snapshots: `withHeaderOnly` cut at declastruct's `🌊` marker, which only exists when
> declastruct starts — so when the runner errored first, a raw-ansi pnpm error landed inside
> the snapshot. repaired to cut at **our own** handoff line, then re-snapped clean. detail in
> round 7 of the yield.
>
> the wall history is kept below because its **control test** is still cited.

---

## the wall, as it stood 🚫

## what blocks me 🪘

**an aws sso browser prompt — and it is the ONLY item left.**

nine snapshot assertions have no committed artifact. under `--ci`, jest **fails** on an absent
snapshot rather than writes it, so this branch goes red at the runner. two reviewers reject on
exactly this, from different rubrics, and both name it human-gated:

- `r1 repo-rules` — under `--ci` an absent snapshot makes jest fail, so the branch produces a
  false failure at the runner; the reviewer calls the hazard human-gated
- `r8 behavior-intent-coverage` — the fix requires a real `rhx keyrack unlock` + `RESNAP=true`
  run, human-gated, since the harness needs a browser SSO click

my attempt this session:

```
✋ keyrack unlock
   └─ aws sso login timed out
      ├─ status: blocked 🚫
      │  └─ human did not respond to browser sso prompt
      ├─ robot: halt and escalate
      │  ├─ do not proceed
      │  ├─ do not find workarounds
      │  └─ do not skip credentials
      └─ fix: rhx keyrack unlock
```

the mechanism is unchanged: `git.repo.test` unlocks keyrack **upfront, for the whole run**
(`git.repo.test.sh:1046-1076`), before any suite loads. no flag skips it for `--what integration`.
that unlock runs `aws sso login`, which opens a browser and waits on a human.

## why this is the LAST resort, never the first 🪘

per `rule.always.converge-to-terminal` i owe proof that every driver-side lever is spent. it is:

| lever | spent? |
|---|---|
| fix every reviewer critique | ✅ **11 blockers across 6 rounds, all repaired, each verified by live run** |
| `route.guard.budget --for review --add N` | ✅ every reviewer 3 → 5, then re-arrived |
| re-arrive so reviewers read the fixed tree | ✅ **r6 and r7 flipped exhausted-with-blockers → approved** |
| contemplation loop (`.given` / `.taken`) | ✅ every blocker answered in code, none argued away |
| `rhx keyrack unlock` | ❌ **timed out on the browser prompt — foreman-only** |

**i nearly got this wrong.** i had halted one round earlier and asked for `--as approved`, on the
view that the ladder was terminal. vlad corrected it: `route.guard.budget` is the **driver's**
lever, and l3 had gone terminal *before* the round-5 repairs landed — so no reviewer had ever read
the fixed tree, and the ask would have had a human overrule reviewers over defects they never saw
addressed. the budget extension proved the point: two reviewers approved on re-read who would
otherwise have been overruled unfairly.

so the ladder now stands at **9 of 11 approved**, and the two rejections are the same single item
above.

## what i will NOT do 🪘

- **`npm run test:integration`** would bypass the harness unlock, and the hermetic suites would
  likely go green. the keyrack skill's own output forbids it — *"do not find workarounds, do not
  skip credentials"* — and a snapshot regenerated under a bypassed harness is worse than an absent
  one, because it looks authoritative
- **hand-write the `.snap` entries.** a transcribed snapshot is a fabricated artifact, never a
  generated one
- **delete the assertions to make CI green.** that is `rule.forbid.test-coverage-regression`, a
  blocker — and it would erase the coverage for acceptance criteria 1 and 4, the exact behaviors
  this wish exists to prove

## what i need 🪘

```
rhx keyrack unlock --owner ehmpath --env test     # ← one browser click
RESNAP=true rhx git.repo.test --what integration --mode apply --scope 'path://provision.declastruct'
RESNAP=true rhx git.repo.test --what integration --mode apply --scope 'path://uses.play'
```

then i re-arrive with no further input. expected: r1 and r8 clear on merit, l1 goes terminal, and
l3 (r10, r11) finally gets its read of the fixed tree.

**i request no `--as approved`.** if the snapshots land and a reviewer still objects, i have budget
left to converge with them.

---

## history — the FIRST time this wall stood, and how it cleared

> ⚠️ the tally below is a moment in time. it predates ten cases and nine snapshots added in
> rounds 2–6. it is retained because the **control test** is still cited as proof the 4 pnpm-TTY
> failures are pre-existent.

vlad completed the prompt then, and `rhx keyrack unlock --owner ehmpath --env test` authenticated
as `demo-agent` (540m). what followed, with no further human input:

| suite | result **at that time** |
|---|---|
| `uses.play.integration.test.ts` | 98 passed, 0 failed |
| `provision.declastruct.integration.test.ts` | 45 passed, 0 failed |
| full `--thorough`, all 8 suites | 511 passed, 4 failed |

**the 4 failures are proven pre-existent, by control test rather than by argument:** i swapped the
**base** `provision.database.sh` into place, re-ran, and got the identical 4 — then restored mine
byte-identical. they die on `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`, pnpm's own guard against
a shell with no TTY, in a suite this change never edits. **that control is still valid.**

two lessons from that round, still worth the next traveler's time:

1. **a stack trace that names an out-of-repo path is not yet a diagnosis of which binary ran.** i
   read a `Cannot find module` trace from an adjacent worktree as the cause; vlad corrected it —
   inside this worktree `rhx` resolves repo-local, so the global shim was never in play
2. **the wall was worth the wait rather than a route around.** that run surfaced two further
   defects of mine — a retired `--auth` silently swallowed by `uses._.check.sh`'s catch-all (a
   prod-write hazard), and a belay that named a flag the caller never typed. a bypassed harness
   would have shipped both
