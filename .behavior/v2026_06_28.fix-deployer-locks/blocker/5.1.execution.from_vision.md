# blocker — 5.1.execution.from_vision

the build is complete and three of five checks pass (types, format, biome-lint). two checks cannot run to green because of **prior environment issues, not this work's code**. both need a human action i cannot perform.

---

## what blocks you

### blocker 1 — keyrack misconfig halts the integration harness

`rhx git.repo.test --what integration` unconditionally runs `rhx keyrack unlock --owner ehmpath --env test` before any test, and exits fatally if it fails (`git.repo.test.sh:1031`). it fails:

```
ConstraintError: key lacks awsSsoUsername metadata; re-set the key
  profileName: ehmpathy.demo
  hint: run: rhx keyrack set --owner ehmpath --env test --key AWS_PROFILE --vault aws.config
```

my new tests (`uses.integration.test.ts`) need **zero** credentials — they run shell skills against `genTempDir` temp repos with an isolated `HOME`. but the harness blocks before it ever reaches them. so i cannot prove them green.

### blocker 2 — third-party `@aws-crypto` cycles fail lint:cycles

`test:lint:cycles` (dpdm) reports circular deps in `@aws-crypto/crc32` and `@aws-crypto/crc32c` — third-party node_modules. these were pulled in by the **pre-staged** dependency bump (`rhachet` 1.41.22 → 1.42.4 and siblings), already staged in `package.json` / `pnpm-lock.yaml` before this session began.

`.dpdmrc.yaml` already excludes `aws-sdk` but not `aws-crypto` (a prior entry). the brief `rule.forbid.dpdm-exclude-change` forbids me from editing the exclude list, and the cycle is inside un-fixable third-party code.

---

## what you tried

- **types** → ✅ `rhx git.repo.test --what types` passed
- **format** → ✅ passed (auto-fixed one long-line wrap in the test via `npm run fix:format`)
- **lint (biome)** → ✅ `Checked 25 files … No fixes applied` (biome stage clean)
- **lint (cycles)** → ⛔ fails only on `@aws-crypto` third-party cycles
- verified my diff adds only `.sh` files + one `.test.ts` — the `.test.ts` is excluded from dpdm's `src/**/!(*.test).ts` scan, so my code cannot introduce these cycles
- verified via `git diff --cached package.json` that the rhachet dep bump (source of the cycles) predates this session
- confirmed the keyrack failure is one malformed key (`AWS_PROFILE` lacks `awsSsoUsername`), and my tests do not use it — the harness simply unlocks all keys up front
- checked the harness for a keyrack-skip flag for integration: none present

## what you need

pick either or both to unblock final verification:

1. **fix the keyrack key** (human-only — needs the AWS SSO username):
   ```
   rhx keyrack set --owner ehmpath --env test --key AWS_PROFILE --vault aws.config
   ```
   then i will run `rhx git.repo.test --what integration --scope 'path://src/domain.roles/deployer/skills/uses'` and prove the 9 cases green.

2. **approve a dpdm exclude addition** for `aws-crypto` (consistent with the prior `aws-sdk` entry), or otherwise decide how to handle the pre-staged dep bump's third-party cycles. since the brief forbids me editing the exclude unprompted, i need your explicit go-ahead.

i have not marked this stone passed: verification strictness forbids claiming green on tests i could not run.