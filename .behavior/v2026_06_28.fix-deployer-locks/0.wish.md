wish =

hey, we gotta add some repo grain meters to allow explicit allow, default block privs to do deployer operations against prod env

prep and test env can be default allow

but prod is sacred. by default, we should never need to apply to prod from local. only under special circumstances

hence, we gotta add a meter with explicit usage allowed 

just like 
- git.commit.uses (from rhachet-roles-ehmpathy)
- radio.uses (from rhachet-roles-bhuild)

we should include both the repo, org, and global scope - just like git.commit.uses

---

lets triage all the skills that then need to conform to this under the deployer too. and ensure that they're all gated at --env prod attempts via this

---

and what should we call the skill, too?

e.g., provision.* and deploy.* both need it



