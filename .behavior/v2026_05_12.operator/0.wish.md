wish =

take a look at ahbode/svc-jobs

we recently released a bunch of generically useful tools for
- query.database
- invoke.command
- invoke.vital
- use.rds.capacity (which itself calls use.vpc.tunnel)
- query.cloudwatch.logs
- query.cloudwatch.metrics
- deploy

and probably some other ones

we want to promote those into generically useful skills here

the query ones fit well into our observer role

the invoke.command one fits well into the operator role (we should add a new role for that)

deploy goes into the deployer, naturally

---

actually, instead of operator, lets make the new role called 'supporter'

---

actually, lets name it operator still. feels more intuitive here

