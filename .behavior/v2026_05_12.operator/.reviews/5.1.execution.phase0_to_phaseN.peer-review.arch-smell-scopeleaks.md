🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-15T19-33-05-557Z
   ├─ review: .behavior/v2026_05_12.operator/.reviews/5.1.execution.phase0_to_phaseN.peer-review.arch-smell-scopeleaks.md
   └─ summary
      ├─ 1 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Scope leak: reach-in import from another domain's internals

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.scope-leaks.md

**locations**:
- src/domain.roles/observer/skills/query.database.ts:20

The code reaches into another domain's internals via dynamic import of getDatabaseConnection from src/utils/database/. This violates bounded context boundaries because the observer role skill must not import or depend on utils/database logic, which belongs to a separate context (likely access or infrastructure layer). Such reach-ins create hidden dependencies and entangle domains that should remain independent. The code should instead use an exported contract or interface from the owning domain rather than a direct path-based import from git root.

**snippet**:
```ts
  const { getDatabaseConnection } = await import(
    path.join(gitRoot, 'src/utils/database/getDatabaseConnection')
  );
```
