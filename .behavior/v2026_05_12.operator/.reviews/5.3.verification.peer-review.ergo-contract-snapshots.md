🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-17T12-44-21-626Z
   ├─ review: .behavior/v2026_05_12.operator/.reviews/5.3.verification.peer-review.ergo-contract-snapshots.md
   └─ summary
      ├─ 1 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Non-exhaustive contract snapshots

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.verification/rule.require.contract-snapshot-exhaustiveness.md

**locations**:
- src/domain.roles/__snapshots__/getRoleRegistry.test.ts.snap
- src/domain.roles/deployer/__snapshots__/getDeployerRole.test.ts.snap
- src/domain.roles/observer/__snapshots__/getObserverRole.test.ts.snap
- src/domain.roles/operator/__snapshots__/getOperatorRole.test.ts.snap

Every user-faced contract must have exhaustive snapshot coverage for all output variants (positive path, negative path, and edge cases). The snapshots in these files only cover a limited set of variants.

- getRoleRegistry.test.ts.snap covers only 'absent role' (edge/negative) and 'the ghlitch role registry' (positive structure). No snapshot for querying an existing role.
- The individual role snapshots (deployer, observer, operator) each cover only the main structure and one edge case, missing other potential variants like error cases or additional edges.

Gaps in snapshot coverage mean that not all caller experiences are proven, allowing potential drift and regressions to go undetected.

**snippet**:
```js
exports[`getRoleRegistry given: [edge] query for absent role when: searched by unknown slug then: it returns undefined 1`] = `undefined`;

exports[`getRoleRegistry given: the ghlitch role registry when: loaded then: it matches the expected structure 1`] = `
{
  "roleCount": 7,
  "roleSlugs": [
    "alerter",
    "budgeter",
    "deployer",
    "detective",
    "hardener",
    "observer",
    "operator",
  ],
  "slug": "ghlitch",
}
`;
```
