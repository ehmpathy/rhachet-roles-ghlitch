🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-15T07-25-01-911Z
   ├─ review: .behavior/v2026_05_12.operator/.reviews/3.3.1.blueprint.product.peer-review.arch-hazards-behavior.md
   └─ summary
      ├─ 5 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Hidden side effects in use.vpc.tunnel.ts

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.behavior-hazards.md

**locations**:
- .behavior/v2026_05_12.operator/3.3.1.blueprint.product.yield.md

The blueprint describes use.vpc.tunnel.ts as calling sdkHosts.setEntry to update /etc/hosts (requiring sudo) as part of establishing the tunnel. This mutates global system state in a way not obvious from the function name 'use.vpc.tunnel'. The rule explicitly calls out hidden side effects (mutations to global state) as behavior hazards that cause intermittent failures and impossible-to-debug incidents.

**snippet**:
```text
├── [+] call sdkSsm.startSession (communicator — start ssm session)
├── [+] call sdkHosts.setEntry (communicator — update /etc/hosts, requires sudo)
└── [+] call sdkSsm.maintainSession (communicator — maintain tunnel until stop)
```

---

# blocker.2: Time assumptions and polling without backoff

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.behavior-hazards.md

**locations**:
- .behavior/v2026_05_12.operator/3.3.1.blueprint.product.yield.md

use.rds.capacity.sh plans to 'wait for db capacity (up to 180s)' after calling the tunnel. This is a fixed time assumption and implicit polling with no mention of backoff, max attempts, or latency handling. The rule forbids time assumptions and polling without backoff because they produce intermittent failures that depend on load or external state and pass tests but fail in production.

**snippet**:
```text
├── [+] call use.vpc.tunnel for db endpoint
├── [+] wait for db capacity (up to 180s)
├── [+] output localhost endpoint
└── [+] keep tunnel active until exit
```

---

# blocker.3: Race condition risk on shared system state

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.behavior-hazards.md

**locations**:
- .behavior/v2026_05_12.operator/3.3.1.blueprint.product.yield.md

Multiple described flows (use.rds.capacity.sh calling use.vpc.tunnel, maintainSession, and keep tunnel active until exit) modify shared state (/etc/hosts, active tunnels, docker containers) with no locks, transactions, or concurrency guards mentioned. Concurrent execution of these skills or multiple instances would create unprotected read-modify-write sequences on global state, exactly the race condition pattern the rule forbids as a blocker.

**snippet**:
```text
├── [+] call use.vpc.tunnel for db endpoint
├── [+] wait for db capacity (up to 180s)
└── [+] keep tunnel active until exit
```

---

# blocker.4: Non-idempotent mutations without guards

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.behavior-hazards.md

**locations**:
- .behavior/v2026_05_12.operator/3.3.1.blueprint.product.yield.md

Deployer and operator skills (deploy.sh, provision.database.sh, provision.terraform.sh, aws.cloudformation.rollback.sh) perform mutations (serverless deploy, terraform apply, continue-update-rollback, docker run) with no described prior-result checks, idempotency keys, or early returns on duplicate calls. Retries or re-execution can duplicate side effects (e.g. duplicate deploys or containers), violating the idempotency violations section of the rule.

**snippet**:
```text
├── [+] run serverless deploy (or npm run deploy)
├── [+] stream logs to stdout
└── [+] report success or failure with next steps
```

---

# blocker.5: Hidden side effect in query.cloudwatch.logs

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.behavior-hazards.md

**locations**:
- .behavior/v2026_05_12.operator/3.3.1.blueprint.product.yield.md

query.cloudwatch.logs.sh is described as performing a write to .agent/.cache/ after the query. The skill name and 'query.*' prefix imply read-only retrieval, yet it has an undocumented write side effect. This matches the rule's blocker example of a 'get' function that also writes, causing unpredictable state and hard-to-debug issues.

**snippet**:
```text
├── [+] run aws logs filter-events
├── [+] format output
└── [+] write cache to .agent/.cache/
```
