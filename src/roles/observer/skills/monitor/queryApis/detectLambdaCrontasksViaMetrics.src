
```
bootup your context via `npx rhachet briefs boot --role mechanic`
```


```
fulfill the acceptance criteria in src/skills/monitor/queryApis/detectLambdaCrontasksViaMetrics.[criteria].md

via commands which leverage the aws cli

into src/skills/monitor/queryApis/detectLambdaCrontasksViaMetrics.ts
```

```
review src/skills/monitor/queryApis/detectLambdaCrontasksViaMetrics.ts

detect violations of rules or patterns

emit to src/skills/monitor/queryApis/detectLambdaCrontasksViaMetrics.[review].i1.md
```

```
apply feedback from src/skills/monitor/queryApis/detectLambdaCrontasksViaMetrics.[review].i1.md
```
