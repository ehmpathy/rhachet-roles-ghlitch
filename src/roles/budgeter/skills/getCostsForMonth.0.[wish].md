[wish] = .sh command which fetches all the costs for the current authed account in last months statement and writes as a json file to the `--output` location specified




> now, enable specification via input of which month to look at; support --month sofar, --month last, --month
2025-09 formats
