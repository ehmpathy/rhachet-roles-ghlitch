1. enable s3tablemetadata
   1. use declastruct pattern
      1. declare the resource; DeclaredAwsS3BucketMetadataConfig
      2. declare the dao; daoAwsS3BucketMetadataConfig
         1. .get({ byUnique })
         2. .set({ upsert/update })
   2. declare "setS3BucketUsageObservability"
      1. declares what we want the config to be
      2. compares to what the config is now (jest diff)
      3. logs whether will update or noop

2. enable s3tablemetadata queries
   1. use declastruct pattern
      1. declare the dependent resources
      2. declare the dao
   2. add to setS3BucketUsageObservability
