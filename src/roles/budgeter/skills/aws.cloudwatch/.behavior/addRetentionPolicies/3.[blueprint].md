use @ehmpathy/as-command

use src/domain/operations and src/domain/objects under src/roles/budgeter/skills/aws.cloudwatch

get and set the policies; set only if `retentionRealized` differs from `retentionDesired`


---


output the final command at src/roles/budgeter/skills/aws.cloudwatch/src/contract/commands/setRetentionPolicies.ts



