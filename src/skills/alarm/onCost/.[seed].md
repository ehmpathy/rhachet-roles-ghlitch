# alarm.on(Cost)

## acceptance.criteria::Behavior

usecase.1('question expense reports')

- given('an aws api key with privs to .get(bills)')
  - when('asked to report expense statistics')
    - then('reports avg and stdev')
    - then('reports stats for each service used')
  - when('asked if sample expense is anomalous for a service')
    - then('answers true for surspend anomaly')
    - then('answers true for subspend anomaly')


usecase.2('question cloudwatch to understand usage')
  - when('asked to detect which behaviors explain expenses')
    - then('queries cloudwatch metrics')

usecase.3(')
  - when('asked to define a cloudwatch alarm for a threshold')
    - then('can do so')
