never query per resource what you can instead query in bulk

query things in bulk; save the results as json to an output file

then do per resource evaluations against the local results table (1 query per dimension, instead of 1 X R resoureces queries per dimension)
